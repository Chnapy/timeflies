import { StateManager } from './StateManager';

export class StateManagerWatch extends StateManager<'watch'> {

    onTileHover(pointer: Phaser.Input.Pointer): void {
    }

    onTileClick(pointer: Phaser.Input.Pointer): void {
    }

    onTurnEnd(): void {
    }
}
