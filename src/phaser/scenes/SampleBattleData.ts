import { LoadSceneData } from './LoadScene';

export const SampleData: LoadSceneData = {
    mapKey: 'sampleMap1',
    battleData: {
        mapInfos: {
            tilemapKey: 'map',
            decorLayerKey: 'view',
            obstaclesLayerKey: 'obstacles'
        },

        players: []
    }
};
