import { MapGraphics } from "./MapGraphics";
import { Scene } from "phaser";
jest.mock('phaser');

describe('#MapGraphics', () => {

    let graphics: MapGraphics;

    it('should return coherent tilemap', () => {

        // const scene = new Scene('key');

        // {
        //     graphics = MapGraphics(
        //         tilemapKey: '',
        //         obstaclesLayerKey: '',
        //         decorLayerKey: '',
        //     },
        //     {
        //         scene
        //     }
        // );

        expect(1 == 1).toBeTruthy();
    });

});
